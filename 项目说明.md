# 个人工作台 · 项目说明

> 这份文档是给「新开对话继续做工具」用的交接说明。
> 新会话开始时，让 AI 先读这个文件，就能接上所有上下文。
>
> 最后更新：2026-07-30

---

## 一句话说明

一个部署在 GitHub Pages 上的个人小工具集合，纯静态、无后端，手机加到主屏后像原生 app。

**线上地址：https://winnicoco99.github.io/**

---

## 关键信息（新会话必读）

| 项 | 值 |
|---|---|
| 本地目录 | `/Users/hb/Desktop/代码/python/轻资产开发/个人工作台/` |
| GitHub 用户名 | `winnicoco99` |
| 仓库 | `winnicoco99/winnicoco99.github.io`（**Public**） |
| 线上地址 | https://winnicoco99.github.io/ |
| 部署命令 | `./deploy.sh "提交说明"` |
| 数据存储 | 浏览器 localStorage，纯前端，不上传 |
| 用户设备 | **Mac + iPhone（Safari）**，已加到主屏 |

**这个目录是独立 git 仓库**，位于「轻资产开发」工作区内部，但和工作区其他内容完全无关。父目录不是 git 仓库，所以不存在公司代码被误提交的可能。

---

## 目录结构

```
个人工作台/
├── index.html          工作台首页，读 tools.js 渲染卡片
├── theme.css           editorial 风格主题，所有页面共用 ← 改样式先看这里
├── tools.js            工具清单 ← 加/下线工具只改这个文件
├── storage.js          localStorage 封装 + 导出/导入备份 ← 新工具直接复用
├── manifest.json       PWA 配置
├── sw.js               Service Worker 离线缓存
├── deploy.sh           一键部署
├── make_icons.py       生成图标（Pillow）
├── README.md           面向使用者的说明
├── 项目说明.md          ← 本文件，面向 AI 的交接文档
├── icons/              icon-192 / 512 / 180.png
└── tools/
    ├── library/        藏书馆（扫码入库 + 查重 + 打卡 + 摘录感想，能用）
    ├── chess/          棋谱库（对局 + 变招，能用）
    ├── shelf/          收藏夹（电影/剧/书 + 日历，能用）
    ├── notes/          随手记（示范工具，能用）
    └── food/           美食地图（已下线，代码保留）

### 藏书馆的文件构成（tools/library/）

```
index.html   骨架 + 样式（三个视图：列表 / 入库 / 详情）
app.js       ES module。列表、打卡、详情、摘录感想、扫码、查重、备份
db.js        IndexedDB 封装（三张表：books / excerpts / thoughts）
vendor/html5-qrcode.min.js  条形码识别，Apache-2.0，375KB
vendor/README.md            许可说明 + 豆瓣接口的实测结论
```

**注意 app.js 是 ES module**（`<script type="module">`），
里面用 `import` 从 db.js 取 DB。加功能时别退回普通 script。
```

### 棋谱库的文件构成（tools/chess/）

```
index.html      骨架 + 棋盘样式
app.js          状态、着法树操作、存储、列表
board.js        棋盘渲染 + 点选走子 + 升变
pgn.js          PGN 解析/导出（含变招）+ 引擎封装层 ← 改这块务必先跑测试
vendor/chess.js chess.js v1.4.0，规则引擎（BSD，加了 UMD 包装）
pieces/*.svg    cburnett 棋子 12 个，就是 lichess 那套（GPL-2.0+）
vendor/README.md 第三方许可说明
```

---

## 加一个新工具（标准流程）

1. **建目录和页面**：`tools/工具名/index.html`
2. **引入公共资源**（路径是两层相对）：
   ```html
   <link rel="stylesheet" href="../../theme.css">
   <script src="../../storage.js"></script>
   ```
3. **在 `tools.js` 里加一条**：
   ```js
   {
     path: 'tools/工具名/',
     name: '显示的名字',
     desc: '一句话说明',
     icon: '◇',        // 从白名单挑，见下文
     tag: ''
   }
   ```
4. **更新 `sw.js`**：把新页面加进 `PRECACHE` 数组，并把 `VERSION` 数字 +1
   （不改版本号，手机上会一直显示旧版）
5. **本地预览**：
   ```bash
   python3 -m http.server 8765     # 在 个人工作台/ 目录下
   open http://localhost:8765
   ```
6. **部署**：`./deploy.sh "加了 XX 工具"`

### 下线一个工具

把 `tools.js` 里对应那段注释掉，代码留着。同时从 `sw.js` 的 `PRECACHE` 移除、`VERSION` +1。
（美食地图现在就是这个状态，想恢复把注释去掉即可）

---

## 设计规范（重要，别破坏）

风格是 **editorial 风**（Anthropic / Claude 风的提炼），来自
`/tmp/editorial/SKILL.md`（源文件 `~/Downloads/editorial-style-design-v1-1.0.0.zip`）。

### 五条核心原则

1. **暖米色基调** `#EFEAE0`，去掉数字屏幕的冷硬感
2. **衬线粗体唱主角**，`font-weight: 900`，字距收紧 `letter-spacing: -1.5px`
3. **橙色 `#CD6F47` 是唯一彩色强调**，需要克制时**减少用量而不是换色**
4. **大量留白**，一屏一观点
5. **无阴影、无渐变、无立体效果**

### 配色变量（都在 theme.css）

```css
--cream:      #EFEAE0   页面底
--cream-deep: #E5DFD2   卡片、色块
--ink:        #1A1A1A   主黑（不用纯 #000）
--text-sec:   #5C5852   次级灰
--orange:     #CD6F47   唯一强调色
--line:       #D6CFC0   分隔线
```

### 字体

```css
--serif: 'Songti SC', 'STSong', 'Source Han Serif SC', 'SimSun',
         'Times New Roman', serif;                                  /* 标题+正文+数字 */
--mono:  'SF Mono', 'JetBrains Mono', 'Consolas', 'Monaco',
         'Courier New', monospace;                                  /* overline、小号数字 */
```

### ⛔ 数字对齐：别把 Georgia 挪回字体栈开头

**这是 2026-08-26 修掉的问题，改字体前先读这一节。**

原来 `--serif` 是 Georgia 打头，它用**旧式数字**：3 4 5 7 9 沉到基线以下、
6 8 冲高。所以任何一串数字（页数、ISBN、日期）看着都一上一下。
用户在手机上看藏书馆详情的页数 / 买入 / ISBN 三行时提出来的。

用 fontTools 量各字体数字 `yMin` 的跨度（归一到 1000/em，越小越齐）：

| 字体 | 跨度 | 结论 |
|---|---|---|
| **Songti SC** | 19 | 完全不下沉，且等宽 ← 现在用这个 |
| Times New Roman | 14 | 齐，但字形单薄，作兜底 |
| Charter | 55 | 3/4/7/9 仍有可见下沉 |
| Georgia | **181** | 问题根源 |

三个必须记住的点：

1. **Georgia 没有 `lnum` 这个 OpenType 特性**，所以
   `font-variant-numeric: lining-nums` 对它是**空操作**。
   这个问题只能换字体，CSS 开关救不回来。
2. **不要用 New York**。它在 macOS 上族名是 `.New York`，
   带前导点的隐藏系统字体，网页 CSS 引用不到。
3. **不要指望 `ui-serif`**。实测在 Chrome / macOS 上它会掉到
   PingFang SC（无衬线），拿不到系统衬线。

最后选宋体同时接管中文和数字 —— 本来就在栈里，数字最齐、等宽，
中英文风格也统一。canvas 逐字量墨迹验证过，底部跨度 2px（Georgia 约 18px）。

`--mono` 那套（Monaco / Menlo / SF Mono / Courier New）数字跨度都在
14~23，本来就齐，不用动。

宋体 Bold 偏细（700 只多约 16% 墨），所以标题的 `font-weight: 900`
是必要的，别降到 700。

### 中文排版的坑（踩过）

**中文大标题 `line-height` 必须 ≥ 1.4。** 英文衬线用 1.05 好看，但中文宋体笔画粗，
1.05-1.2 会让笔画互相压迫，挤成一坨。

### overline 元素

editorial 风很标志性的东西 —— 标题上方一行等宽字体、全大写、`letter-spacing: 2px` 的小标签。
每个页面都该有（首页是 `WORKBENCH`、随手记是 `NOTES`）。

### 图标：禁止 emoji

用抽象几何符号。原则是**抽象优于具象、几何优于拟物、静默优于喧闹**（`⊞` 优于 🏠）。

```
几何：⊞ 模块  ⊕ 增加  ◎ 焦点  ◐ 节奏  ◇ 概念  ◈ 价值  ▣ 容器  △ 方向  ◯ 循环
代码：{ } 结构  < > 比较  [ ] 待办  → 推进  ↗ 增长  ⇄ 双向
排版：§ 章节  ¶ 论述  † 注释  ※ 重点
```

**明确禁止**：🚀 ✨ 🔥 💡 ❤️ 👀 ⚡ 🎯 ✅ ❌ 💯 🎉 👍 —— 这些是「营销 emoji」，
一上就破坏 editorial 感。

两种用法：
- `icon-tile` —— 橙色圆角方块 + 白色符号，每页最多 1 个，放标题区
- `card-mini-icon` —— 无背景的黑色符号，**永远黑色不跟着换色**

---

## 数据存储

### 两套并存：localStorage（多数工具）+ IndexedDB（藏书馆）

藏书馆单独用 IndexedDB，因为 **localStorage 的 5MB 装不下长期累积的摘录**。
实测算过（按 UTF-8 真实字节）：

| 场景 | 占用 | 占 5MB 额度 |
|---|---|---|
| 200 本 + 每本 5 条摘录 1 篇感想 | 0.98 MB | 20% |
| 500 本 + 每本 10 条摘录 2 篇感想 | 4.78 MB | **96%** |
| 1000 本 + 每本 20 条摘录 3 篇感想 | 17.3 MB | **撑爆** |
| 光封面图 200 本（180px） | 3.21 MB | 64% |

结论：文字撑得住，**真正吃空间的是封面图**。藏书馆最终完全不做封面
（见下文原因），并且换了 IndexedDB（容量几百 MB 到 GB 级），
所以摘录想写多少写多少。

新工具怎么选：只存少量结构化数据 → 用 `storage.js`；
可能长期累积长文本 → 抄 `tools/library/db.js`。

### 现状：纯 localStorage

用 `storage.js` 里的 `Store` 类：

```js
const store = new Store('工具名');      // key 变成 workbench.工具名
let items = store.load([]);            // 读，带默认值
store.save(items);                     // 写，返回是否成功
store.exportFile();                    // 导出 json 文件
store.importFile(data => { ... });     // 选文件导入
```

`Store` 已经处理了：隐私模式禁用存储、数据损坏、导入了别的工具的备份（会提示）。

### 会丢数据的情况（已告知用户）

| 情况 | 结果 |
|---|---|
| 正常使用、手机重启、iOS 升级 | 数据在 |
| 清除浏览器数据 | **丢** |
| 删掉这个 PWA | **丢** |
| 手机存储告急被系统回收 | **可能丢** |
| 换手机 | **不跟着走** |

**所以：任何存数据的工具都必须做导出/导入。** 这是硬要求，不是可选项。
参考 `tools/notes/index.html` 底部的 BACKUP 区，直接抄。

### iOS Safari 的存储特性（用户是 iPhone，这条很重要）

**1. 主屏 app 和 Safari 里可能是两份数据。**
iOS 上把网页「添加到主屏幕」后，它跑在独立的 WebView 容器里。
历史上主屏 app 和 Safari 各有一份 localStorage，互相看不见。
新版 iOS 已经统一了，但**不同 iOS 版本行为不一致**。

实际影响：如果用户说「我存的棋不见了」，先问一句
**是从主屏图标进的还是在 Safari 里打开的**，很可能只是看错了地方。

**2. 七天不用可能被清掉。**
Safari 的 ITP（防跟踪机制）会清理「七天内没有交互过的站点」的本地存储。
主屏 app 形式通常不受影响，但纯 Safari 访问有风险。

实际影响：**定期导出备份对 iPhone 用户比 Android 更重要**，不是可选项。

**3. Mac 和 iPhone 之间不会自动同步。**
即使都是 Safari、都登录同一个 Apple ID 也不会。iCloud 同步的是书签和标签页，
不是网站的 localStorage。要跨设备只能靠导出/导入，或者加后端。

### 想要跨设备同步怎么办

用户目前接受手动导出。如果之后要真同步，选项：

- **Supabase 免费层** —— 加登录、数据存云端，改完自动同步。推荐路线
- **GitHub 私有仓库当数据库** —— 免费、有版本历史，但**前端要放 Token，
  当前仓库是 Public，绝对不能这么做**

---

## 已知问题 / 踩过的坑

### 1. 地理编码：Nominatim 对中国地址不可靠

美食地图用过 Nominatim（OpenStreetMap 免费服务，无需 key）。
**实测「浦东新区巨峰路111号」定位到了海宁市** —— 它不理解中国行政层级，会张冠李戴。

结论：如果新工具要用地图，**把「在地图上手动点选」做成主要方式**，
搜索只当辅助且必须让用户核对。想要精准就得申请高德 Web 服务 key（免费）。

### 2. 地图瓦片

用高德瓦片（中文标注、国内快），加载失败自动切 OpenStreetMap：

```js
'https://webrd0{s}.is.autonavi.com/appmaptile?lang=zh_cn&size=1&scale=1&style=8&x={x}&y={y}&z={z}'
```

Leaflet 容器要加 `z-index: 0`，否则会盖住页面其他元素。

### 3. GitHub Pages 在国内访问不稳

`github.io` 在国内移动网络下偶发超时（curl 会返回 000）。这是网络链路问题，不是配置错误，
重试就好。如果用户抱怨太慢，可以考虑换 Cloudflare Pages。

### 4. Service Worker 缓存

改了文件后手机上还是旧版 → `sw.js` 的 `VERSION` 数字 +1 再部署。
即使这样也可能需要**刷新两次**（第一次触发更新，第二次生效）。

### 5. 网页上传文件夹

如果要用 GitHub 网页上传，**必须从 Finder 拖到虚线区域**，
`choose your files` 按钮不支持选文件夹。现在有 `deploy.sh` 了，一般用不上。

### 6. 工作区文件写入限制

AI 的文件写入工具**只能在工作区目录内操作**，`~/Desktop` 下会被拒绝。
所以这个项目放在工作区里而不是桌面。

顺带：`/tmp` 也写不了。临时测试文件得放工作区里（比如 `_tmp_xxx/`），
它们在「轻资产开发」根目录下，不属于 `个人工作台/` 这个 git 仓库，
不会被误提交。

### 7. bash 的 cwd 参数有时不生效

用 `cwd: 个人工作台` 执行 `git status` 时，实际跑在了工作区根目录，
结果报「not a git repository」，看着像仓库丢了，其实没丢。

**对策**：git 命令用 `git -C /绝对路径/个人工作台 status` 这种形式，
或者在命令里直接用绝对路径，不依赖 cwd。

### 8. PGN 的变招括号位置（写解析器必看）

```
1. e4 e5 2. Nf3 (2. Bc4 Bc5) Nc6
```

括号里的 `Bc4` 是**替代 `Nf3` 这一步**的，所以它挂在 Nf3 的
**父节点**下，和 Nf3 是兄弟，不是 Nf3 的子节点。

我第一版导出时把括号写在了被替代着法的**前面**，
导出的 PGN 再解析回来变招就全丢了。正确做法是：
「替代我的兄弟变招」由上层传进来，输出自己之后立刻写括号。

另外，`chess.js` 这类库自带的 PGN 解析**只保留主变，直接丢掉变招**，
所以解析和导出都得自己写，库只当规则裁判。

### 9. chess.js 版本选择

一开始用 0.11.0（顶层 `var Chess`，script 引入即全局，很方便），
但**性能不行**：解析一局 87 手要 490ms，批量导 10 局 1.7 秒，手机上明显卡。

换成 **1.4.0 快一个数量级**（单局 23ms，20 局 174ms）。代价是两个破坏性变化：

| | 0.11.0 | 1.4.0 |
|---|---|---|
| 非法 FEN / 着法 | 返回 false / null | **抛异常** |
| 判定方法名 | `in_checkmate()` | `isCheckmate()` |
| 宽松解析参数 | `{sloppy:true}` | `{strict:false}` |
| 构建格式 | 顶层 var | CommonJS，**需自己加 UMD 包装** |

差异都收在 `pgn.js` 顶部的 `PGN.Engine` 封装层里（`create/move/validFen/status/destinations`），
以后换版本只改那一处。**不要绕过封装层直接 new Chess()**，
否则非法输入会抛异常炸掉页面。

### 10. 着法列表的间距要用真实空格

用 CSS `margin` 控 inline 元素间距，**换行处会失效**，
渲染出 `1.e4e52.Nf3` 这种粘连。改成在 DOM 里插
`document.createTextNode(' ')` 才可靠。

---

## 安全注意事项

1. **仓库是 Public**，所有代码公网可见。别往里放任何密钥、Token、私密内容。
2. **父工作区里有敏感内容** —— `.kiro/steering/` 里有邮箱授权码和钉钉机器人 token，
   `aaa数据地图/` 和各种 SQL 里是公司数据。这些**绝对不能进这个仓库**。
   `个人工作台/` 是独立 git 仓库，正常操作不会碰到，但别手动 copy 文件进来。
3. **git 凭证在 macOS 钥匙串里**（`credential.helper=osxkeychain`），不在项目文件中。
4. **用户的 GitHub Token 曾在对话中明文出现过**，已建议用户撤销重建。
   如果用户还没换，可以提醒一次。

---

## 用户偏好（观察到的）

- 喜欢 Claude / editorial 那种暖米色 + 衬线体的质感，明确说过「差点 claude 的味道」
- 关心数据安全，问过「会永久储存吗」「不同人打开会不会看到彼此数据」
- 不是程序员，但能跟着做 —— 讲清楚点哪里、填什么就行，不用回避技术细节但要解释清楚
- 希望工具**记录类的能编辑**，不只是增删
- 做工具时会先问「能做到吗」，给方案对比比直接开做更受欢迎

---

## 当前状态

### 线上在跑

- 工作台首页
- **藏书馆**（`tools/library/`）—— 见下面「藏书馆现状」
- **棋谱库**（`tools/chess/`）—— 见下面「棋谱库现状」
- **收藏夹**（`tools/shelf/`）—— 见下面「收藏夹现状」
- 随手记（`tools/notes/`）—— 增删、打勾、导出/导入备份

### 收藏夹现状（2026-08-26 改造，未部署）

**目的**：把观影、读书、听播客这些事收在一处，两个方向都能查 ——
从内容查「我什么时候看的、当时怎么评价」，从某一天查「这天都看了什么」。

**这轮改了四件事**

1. **去掉封面，换成紧凑目录。** 跟藏书馆同一套 `.it-dot` / `.it-main` /
   `.it-meta` 结构，单行从 74px 压到 46px，一屏扫十几条。
   状态用前置色点（橙色=在看）。不用再找图上传，也不用碰豆瓣防盗链。
   老数据里的封面 base64 在首次加载时直接丢弃并写回，把 localStorage 放出来。
2. **豆瓣改成存链接，不存图。** 「找豆瓣」通了就把名字和链接一起带回来。
   代理挂了就点「去豆瓣搜」手动粘链接，记录本身不受影响。
   只要 JSON 不要图，链路比原来短一半。

   **行尾的豆瓣入口每条都有**，不依赖有没有存过链接：存了就直达条目页，
   没存就按标题去豆瓣搜（书籍走 `book.douban.com`，其余走 `movie`），
   虚线框表示这是"猜的"。这样老记录不用先补链接就能跳。
   实现见 `doubanLink()`，列表和月历面板共用。
3. **日期升级成打卡数组。** 电影填一个日期就是一天看完；剧和书填「到」，
   保存时摊成整段打卡，之后能在详情日历里逐天增减，也能用「补一段」批量补。
   断续追剧（追两集停一周）就是数组里几个不连续的日子。

   **打卡只在详情页的日历上做**，列表行不放打卡按钮 ——
   清单是用来扫读和找东西的，在那儿放个改数据的按钮容易误触，
   而且行尾位置留给豆瓣跳转更有用。
4. **新增条目详情页。** 标题、分类、状态、评分、时间跨度、豆瓣链接、
   评价正文，加一份这条自己的打卡日历。这是「从内容定位到观看时间和评价」
   的落点。

**两个实现细节，改的时候别踩**

- **单日期是数组长度为 1 的特例**，没有第二套逻辑。`date → dates` 的迁移
  写在 `normalize()` 里，导入老备份也走这个函数，所以老数据和老备份都兼容。
- **编辑时范围没变就不重算打卡。** 手点出来的断续日期不能被拉平成连续一段，
  `f-save` 里靠比对首尾（`sameSpan`）判断；单日保存时如果原来有多天打卡，
  只保证这天在里面，不清掉其它。这两处是回归测试重点。

**合并导入按「标题+分类」认同一条**，把两边打卡日期并起来，
评价/链接/评分谁有取谁，不会因为同一部剧记了两次就出现重复行。

### 藏书馆现状（2026-08-26 完成，未部署）

**为什么做**：家里买了什么书记不清，容易买重。所以
**「扫码 → 立刻告诉你重不重」是第一优先级**，其余功能都排在它后面。

**已完成**

- **扫条形码入库** —— `html5-qrcode` 开摄像头识别 EAN-13。
  限定只认 EAN-13/EAN-8/UPC-A，减少误识
- **查重** —— ISBN 精确匹配优先；无 ISBN 的老书退回「书名+作者」软匹配。
  命中就弹橙色警告，带买入日期/状态/已读天数，可直接跳过去看那本。
  仍然允许强行存第二本（不同版本、要送人）
- **书目自动填充** —— 豆瓣接口，实测填出书名/作者/译者/出版社/出版时间/页数
- **打卡制阅读记录** —— 每本书自己一份日历，点一天=那天读了。
  一天可以给多本书各打一次，可补记过去某天，重复点是取消，
  未来日期禁用。未看的书一打卡自动变「在看」
- **摘录**（原文 + 页码 + 我的批注）和 **感想**（一本书可多篇），都能改能删
- 三种状态：未看 / 在看 / 已看。列表里在看的排最前
- 导出/导入备份，格式对齐 storage.js（`{tool, data, ...}`）
- 显示已用空间，让用户对「还能写多少」有数

**明确没做，以及为什么**

- **完全不做封面图。** 两个原因叠加：
  1. **取不到** —— 豆瓣图片是 **referer 白名单**：带 `book.douban.com`
     的 referer 返 200，空 referer 返 **418**。浏览器不允许网页伪造
     referer，图片代理（weserv 等）也一样被拒
  2. **用户明确不要** —— 预计录几百本，纯文字列表滚动扫读更快

  去掉封面后列表**单行从 71px 压到 43px**（省 38%），一屏能看的书量翻倍。
  状态改用一个前置色点表示（橙色=在看），书名和作者/出版社挤在同一行，
  靠字号和灰度分层次。`.bk-dot` / `.bk-main` / `.bk-meta` 就是这套。

  **别再加回封面** —— 加回来就得重新掉进 referer 那个坑，
  而且行高会弹回去。
- **不扫二维码。** 中文书上的二维码是出版社公众号/正版验证/电商链接，
  **里面没有书目数据**。能查到书的是条形码（EAN-13 = ISBN）。
  从电商页面反查理论可行但要爬页面，反爬严、结构常变，不做
- 不做 OCR 摘录。免费 OCR 对书页小字识别率不行，先手打

**豆瓣接口的实测结论（重要）**

- `api.douban.com/v2/book/isbn/{isbn}?apikey=...` **支持 JSONP**（`callback` 参数），
  所以纯静态页面能直接调，**不需要 CORS 代理**。
  这点比收藏夹强 —— 收藏夹的 suggest 接口只能走公共代理，
  实测那些代理经常整条挂掉（522）
- 命中率约八成。实测 6 本命中 5 本，连 2026-07 刚出的新书都有。
  查不到的是冷门书/港台原版，所以**手动录入必须始终可用**
- 也支持按书名搜（`/v2/book/search`），扫不到码时的兜底
- 这是非官方开放接口，可能哪天就变。变了不影响已存数据，只是新书得手填

**测试**

`_tmp_lib_test/`（不在本仓库，用完可删）里有 playwright 测试，
上线前 **35 项全绿**：IndexedDB 刷新持久、同一天多本书打卡互不干扰、
重复点取消、补记过去、删书连带清理摘录感想、导出导入 round-trip
不产生重复、390 宽不横向溢出、无 JS 报错。

改 db.js 或打卡逻辑前**务必先把测试跑起来**。这类 bug 很隐蔽
（数据静默丢失、孤儿记录），肉眼看不出来。

```bash
python3 -m http.server 8792     # 在 个人工作台/ 下
cd _tmp_lib_test && node test.mjs
```

注意：测试用 `chromium.launch({ channel: 'chrome' })` 借系统 Chrome，
因为 `npx playwright install chromium` 在这台机器上会失败
（本地缓存的浏览器版本号和 npm 装的 playwright 不匹配）。

**待观察**

摘录多了以后详情页会很长（摘录和感想都是全量展开的）。
可能需要折叠或分页，等实际攒了几十条再看，别凭猜加。

### 棋谱库现状（2026-07-30 上线）

**已完成**

- 棋盘：lichess 的 cburnett 棋子，配色改成工作台的暖色系。
  点起点→显示合法落点→点终点落子；升变弹窗选后车象马；
  上一步高亮、被将的王画红圈；可翻转视角
- PGN 粘贴导入：**变招、注释、`!?` 批注、`[%clk]` 时钟全部保留**。
  黑白方/日期/结果/ECO 自动填表单。一次粘多局会拆开分别保存
- 手动录入 + 变招：在任意着法上走一步不同的棋，自动开分支，
  主变不动。可「升为主变」互换，可删除整段子树
- 导航：首/上/下/末 + 翻转，桌面支持方向键
- 防丢棋：切换棋局/清空/导入前都会确认，另有 beforeunload 兜底
- 导出：整库 JSON 备份 + 标准 PGN（带变招，能导进 lichess / ChessBase）

**还没做（第二期）**

- 筛选框（对手、结果、执黑执白、开局、标签、日期区间）
- 日历视图（哪天下了什么棋）
- 局面 tab（现在是占位文案，计划支持摆子 + FEN 收藏）
- PGN 文件上传（现在只能粘贴）

**待观察的体验问题**

87 手的棋谱着法列表内容高约 500px，可视区只有 300px，
手机上翻找中局某一手要滚一段。可能需要「只看主变」开关或可拖高度，
等实际用过再定，别凭猜加。

### 已下线但代码保留

- 美食地图（`tools/food/`）—— 完成了记录 CRUD、地图打点、
  同店多次访问合并成一个标记、悬浮看详情、编辑。
  下线原因：用户想先做别的工具。恢复方法见上文「下线一个工具」。

### 下一步

1. **藏书馆部署 + 手机实测扫码**。`deploy.sh "加了藏书馆"` 之后
   必须在 iPhone 上拿真书扫一次 —— Mac 摄像头拍条形码吃力，
   还没在真机上验证过。摄像头需要 HTTPS，GitHub Pages 满足
2. 棋谱库第二期：筛选 + 日历 + 局面 tab。优先级等用户实际用过再定

用户明确说过「对棋谱工具不熟」，所以做第二期时**先问清楚他实际怎么用**，
不要凭想象加功能。

### 棋谱库的测试（重要）

改 `pgn.js` 或 `board.js` 前，**务必先把测试跑起来**。着法树和 PGN
解析的 bug 都很隐蔽（变招静默丢失、导出后结构变形），肉眼看不出来。

测试脚本这次放在工作区根的 `_tmp_chess_test/`（不在本仓库里，用完删了）。
如果需要重建，覆盖这些点就够：

1. **解析**：主变、头部标签、变招挂载位置、嵌套变招、注释/NAG、
   王车易位/升变、自定义 FEN
2. **round-trip**：解析 → 导出 → 再解析，用「结构指纹」
   （递归拼 san + 子树）比对两棵树必须完全一致。这条最能抓 bug
3. **容错**：非法着法、未闭合括号/注释、无回合号、脏空白
4. **性能**：真实名局（87 手）round-trip < 150ms，20 局导入 < 1.5s
5. **浏览器端**（playwright）：点选走子、**在已有着法上走不同的棋
   必须开分支且主变不变**、升变弹窗、保存后刷新不丢、移动端棋盘不溢出
6. **防丢棋**：脏状态下切换棋局/清空/导入都要弹确认，取消后棋还在

上线前这四套（解析 53 项、边界 46 项、浏览器 84 项、防丢棋 24 项）
和线上验证 23 项都是全绿的。

---

## 新会话开场建议

新对话可以这么开：

> 读一下 `个人工作台/项目说明.md`，我要在这个工作台上加一个新工具：XXX

AI 读完这份文档就能直接动手，不用重新解释背景。
